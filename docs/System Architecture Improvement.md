System Architecture Improvement
- Integrate faceID at enrollment instead of just image upload for more accurate detection like apple with truedept technology
- Optimize real time face recognition by implementing a more efficient embedding model and leveraging GPU acceleration.
- Do face detection on frontend and do recognition on backend with that one frame so the steam doesnt looks lag
- Check if vector detect only the face or the whole image
- For Chatbot, check if it's in one session each chat